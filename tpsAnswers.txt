TPS Activity 1

1.  Name the 3 pools for memory and what kind of variables will be stored in each pool.
    The three pools of memory are stack, heap, and static/data segment. The stack stores local variables or function parameters. The heap holds allocated memory. the data segment stores static or gloabl variables that last through the whole program.

2. 
    a. There are 3 varaibles that are declared
    b. There are 2 pointers declare. ptr points to the memory address of an int and handle points to a pointer
    c. the varibles are stored in the stack
    d. ptr will point to a section on the heap when malloc is called

TPS Activity 2

1. In NodeStruct there is a variables named head that points to a struct and dynamically allocates the needed space for it

3. The pointer where head is stored is no where near where the Node structure is. However the data inside the Node structre are together.



